# homebrew-homebrew

## Usage

    brew tap robertjlooby/homebrew
    brew install <formula>

### Creating bottles

    brew uninstall <name>
    brew install --build-bottle <name>
    brew bottle <name>
